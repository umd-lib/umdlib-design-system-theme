$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: UMD Lib Accordion Main
label: Accordion
group: UMD Libraries

props:
  type: object
  properties:
    componentid:
      type: string
      title: Component ID
      description: (Optional) An ID to use for the component. This can be used for section referencing.

slots:
  accordion_child_slot:
    title: Accordion Child (UMD Libraries Accordion)
    description: Slot for the UMD Libraries Accordion component.
